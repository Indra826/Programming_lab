#include<stdio.h>
int fib(int a,int b,int n);
int main()
{int n;
     scanf("%d",&n);
printf("%d ",fib(0,1,--n));
return 0;
}
int fib(int a,int b,int n)
{if(n==0)
return ;
printf("%d ",a);
return fib(b,a+b,--n);
}
